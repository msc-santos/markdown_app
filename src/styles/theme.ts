import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#eceeee',
      main: "#0d1117",
      dark: '#0d1117',
      contrastText: '#ffffff',
    },
    secondary: {
      main: "#7356b5"
    },
  }
});